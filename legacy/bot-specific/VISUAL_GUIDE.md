#!/usr/bin/env markdown
# 📊 EditorBot Visual Architecture

## Message Flow Visualization

```
                    TELEGRAM SERVER
                    ┌──────────────┐
                    │  User sends  │
                    │ voice/text   │
                    └──────┬───────┘
                           │
                    ┌──────▼────────┐
                    │  Polling      │
                    │  (long poll)  │
                    └──────┬────────┘
                           │
         ┌─────────────────┼─────────────────┐
         │                 │                 │
    ┌────▼────┐      ┌─────▼──────┐    ┌────▼────┐
    │ VOICE   │      │    TEXT    │    │ COMMAND │
    │         │      │            │    │         │
    │voice.py │      │ text.py    │    │commands │
    │         │      │            │    │ .py     │
    └────┬────┘      └─────┬──────┘    └────┬────┘
         │                 │                 │
         └─────────┬───────┴────────┬────────┘
                   │                │
         ┌─────────▼────────┐       │
         │                  │       │
         │ STATE MACHINE    │◄──────┘
         │ (FSM)            │
         │                  │
         │ machine.py       │
         └─────────┬────────┘
                   │
         ┌─────────▼────────────────┐
         │                          │
         │ SERVICES LAYER           │
         │ ┌──────────────────────┐ │
         │ │ transcription.py     │ │
         │ │ (audio → text)       │ │
         │ └──────────┬───────────┘ │
         │            │             │
         │ ┌──────────▼───────────┐ │
         │ │ mediation.py         │ │
         │ │ (dialect → standard) │ │
         │ └──────────┬───────────┘ │
         └────────────┬──────────────┘
                      │
         ┌────────────▼────────────────┐
         │                             │
         │ STORAGE LAYER               │
         │ runtime.py                  │
         │ ┌──────────────────────┐    │
         │ │ get_conversation()   │    │
         │ │ save_conversation()  │    │
         │ └──────────────────────┘    │
         │ (in-memory dict for now)    │
         │ (replace with DB in prod)   │
         └────────────┬─────────────────┘
                      │
         ┌────────────▼──────────────┐
         │                           │
         │ RESPONSE FORMATTING       │
         │ (create bot reply)        │
         │                           │
         └────────────┬──────────────┘
                      │
         ┌────────────▼──────────────┐
         │                           │
         │ TELEGRAM API              │
         │ send message              │
         │                           │
         └────────────┬──────────────┘
                      │
         ┌────────────▼──────────────┐
         │                           │
         │ USER SEES RESPONSE        │
         │                           │
         └───────────────────────────┘
```

---

## State Machine Detailed Diagram

```
                START
                  │
                  ▼
            ┌─────────────┐
            │    IDLE     │  (waiting for voice)
            └──────┬──────┘
                   │ VOICE_RECEIVED
                   ▼
            ┌──────────────────┐
            │  TRANSCRIBED     │  (voice → text)
            └──────┬───────────┘
                   │ TEXT_RECEIVED (transcript)
                   ▼
            ┌──────────────────┐
            │   MEDIATED       │  (text → mediated)
            └┬──────────────┬──┘
             │ COMMAND_OK   │ TEXT_RECEIVED
             │              │ (mediated text)
             │              ▼
             │        ┌───────────────────┐
             │        │  AWAITING_EDIT    │◄──┐
             │        │                   │   │
             │        │ (user editing)    │   │
             │        └┬──────────────┬───┘   │
             │         │              │       │
             │    OK   │       TEXT   │       │
             │         │      RECV    │       │
             │         │              └───────┘
             │         ▼
             │    ┌──────────────────┐
             │    │   CONFIRMED      │  (user confirmed)
             │    └──────┬───────────┘
             └───────────┼────────────┘
                         │
                         ▼
                    ┌─────────────┐
                    │    IDLE     │  (ready for next)
                    └─────────────┘
                         │
                    (cycle repeats)
```

---

## Component Dependency Graph

```
                    telegram library
                           │
           ┌───────────────┴────────────────┐
           │                                │
        bot.py                          handlers/
        │                                 │
        ├─ ApplicationBuilder          ├─ voice.py ──┐
        │                              │              │
        ├─ MessageHandler          ├─ text.py   ├───┤
        │                              │              │
        └─ run_polling()            └─ commands.py   │
                                                     │
                                        ┌────────────┘
                                        │
                                    state/
                                    │
                              ┌─────┴──────────┐
                              │                │
                        machine.py       runtime.py
                              │                │
                            models.py◄────────┘
                              │
        ┌─────────────────────┘
        │
    services/
    │
    ├─ transcription.py ─────┐
    │                        │
    └─ mediation.py ────────►├─ dialect_mediator
                             │
                      genai (Google API)
```

---

## Data Structure Relationships

```
┌────────────────────────────────┐
│   Telegram.Update              │
│ (received from Telegram)       │
│                                │
│  ├─ effective_chat.id          │
│  ├─ message.text               │
│  ├─ message.voice              │
│  └─ ...                         │
└────────────┬───────────────────┘
             │
             ▼
┌────────────────────────────────┐
│   Conversation (our dataclass) │
│ (state representation)         │
│                                │
│  ├─ state: BotState            │
│  ├─ transcript: str            │
│  └─ mediated_text: str         │
└────────────┬───────────────────┘
             │
             ├──► Machine Logic
             │    │
             │    └─► Validates transitions
             │        Returns new Conversation
             │
             └──► Runtime Storage
                  │
                  ├─► _conversations[chat_id]
                  │
                  └─► (future: Database)
```

---

## Request Processing Timeline

```
TIME    EVENT                  HANDLER          STATE              STORAGE
────────────────────────────────────────────────────────────────────────────
0ms     User taps mic
        ├─ Audio buffering
        └─ Sends 5 sec voice
        
50ms    Telegram API receives message

100ms   Bot receives Update    ─────────────────
        ├─ Detects VOICE       voice.py
        └─ Creates handler

110ms   Download audio file    ─────────────────
        ├─ ~2-3MB
        └─ Save to temp

120ms   VOICE_RECEIVED event   machine.py       IDLE → TRANSCRIBED
        ├─ Validate            (event handler)
        └─ Create new state                    

140ms   Transcription starts   transcription.py ─────────────────
        ├─ Audio processing
        └─ API call (Whisper) [2000ms]

2140ms  Transcription result   ─────────────────  save state
        ├─ "Che, como estah?"  machine.py       TRANSCRIBED → MEDIATED
        └─ TEXT_RECEIVED                        
                                                  runtime.py
2150ms  Mediation starts       mediation.py     ─────────────────
        ├─ Create mediator
        ├─ Load dialect profile
        └─ API call (Gemini)  [1500ms]

3650ms  Mediation result       ─────────────────  save state
        ├─ "Oye, ¿cómo estás?" machine.py       MEDIATED → AWAITING_EDIT
        └─ TEXT_RECEIVED                        
                                                  runtime.py
3660ms  Send response          handlers/voice.py ─────────────────
        ├─ Format message
        ├─ Include options
        └─ Call Telegram API   [100ms]

3760ms  User sees response     ─────────────────
        ├─ Mediated text
        ├─ Options
        └─ Ready for action

3800ms+ User taps OK/EDIT/     ────────────────► handlers/text.py
        CANCEL
                               ─────────────────► machine.py
                                                  runtime.py
                               ─────────────────► Telegram API

TOTAL TIME: ~3.7 seconds from voice to response
```

---

## Adding a Feature: Step-by-Step

```
┌──────────────────────────────────────────┐
│ 1. DEFINE STATE (if needed)              │
│    └─ Add to BotState enum               │
│       (state/models.py)                  │
└──────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────┐
│ 2. DEFINE EVENTS (if needed)             │
│    └─ Add to EventType enum              │
│       (state/machine.py)                 │
└──────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────┐
│ 3. UPDATE DATA MODEL (if needed)         │
│    └─ Add fields to Conversation         │
│       (state/models.py)                  │
└──────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────┐
│ 4. ADD STATE TRANSITIONS                 │
│    └─ Update handle_event() function     │
│       (state/machine.py)                 │
└──────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────┐
│ 5. IMPLEMENT SERVICE (if needed)         │
│    └─ Create or update service           │
│       (services/)                        │
└──────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────┐
│ 6. UPDATE HANDLER                        │
│    └─ Call new transitions/services      │
│       (handlers/voice.py or text.py)     │
└──────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────┐
│ 7. TEST                                  │
│    └─ Verify state machine               │
│    └─ Test service directly              │
│    └─ Run full flow                      │
└──────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────┐
│ ✅ FEATURE COMPLETE                      │
└──────────────────────────────────────────┘
```

---

## Error Handling Flow

```
┌─────────────────────────────────────────────┐
│ Operation (e.g., transcribe_audio)          │
└──────────┬──────────────────────────────────┘
           │
    ┌──────▼───────┐
    │ Success?     │
    └┬────────────┬┘
     │ NO         │ YES
     │            └────────►┌─────────────────┐
     │                      │ Return result   │
     ▼                      │                 │
┌──────────────────┐        └─────────────────┘
│ Exception caught │
│                  │
│ - Log error      │
│ - Map to user    │
│   message        │
│ - Send to user   │
│                  │
│ await reply_text │
│  (⚠️ Error: ...) │
└──────────────────┘
```

---

## Class Relationships

```
┌──────────────────┐
│   BotState       │ ◄── Enum with 5 states
│  (enum)          │     (IDLE, TRANSCRIBED, etc.)
└──────────────────┘
         ▲
         │ used by
         │
┌──────────────────────────────┐
│   Conversation               │ ◄── Dataclass
│  (data container)            │     Stores one user's
│                              │     conversation state
│  ├─ state: BotState          │
│  ├─ transcript: str | None   │
│  └─ mediated_text: str | None│
└──────────┬───────────────────┘
           │
           ├── created by: machine.py
           │
           ├── stored by: runtime.py
           │
           └── returned by: get_conversation()

┌──────────────────┐
│   EventType      │ ◄── Enum with 5 event types
│  (enum)          │     (VOICE_RECEIVED, etc.)
└──────────────────┘
         ▲
         │ used by
         │
┌──────────────────────────────┐
│   handle_event()             │ ◄── Pure function
│  (FSM function)              │     state → event → new state
│                              │
│  Input:                      │
│  ├─ convo: Conversation      │
│  ├─ event: EventType         │
│  └─ payload: str | None      │
│                              │
│  Output: new Conversation    │
│                              │
│  Raises: InvalidTransition   │
└──────────────────────────────┘
```

---

## File Edit Hotspots

```
To change:                   Edit file:              What to look for:
─────────────────────────────────────────────────────────────────────────
Bot messages                 handlers/*.py           reply_text("...")

Dialect logic                services/mediation.py   VenezuelanDialectProfile()

FSM states                   state/machine.py        if state == BotState.X:

Data stored                  state/models.py         @dataclass Conversation

Telegram connection          bot.py                  ApplicationBuilder()

User commands                handlers/text.py        if text.upper() == "..."

Audio processing             services/transcription  transcribe_audio()

State storage                state/runtime.py        _conversations dict
```

---

## Debug Recipe

```
Problem: Bot not responding to voice

Step 1: Check connection
        echo $TELEGRAM_BOT_TOKEN
        → If empty, set it

Step 2: Check token validity
        python -c "from telegram.ext import ApplicationBuilder"
        → If error, token might be wrong

Step 3: Check audio download
        Add: logger.debug(f"Downloaded: {tmp.name}")
        → Check if file created

Step 4: Check transcription
        Add: logger.debug(f"Transcript: {transcript}")
        → Check if text returned

Step 5: Check mediation
        Add: logger.debug(f"Mediated: {mediated}")
        → Check if GEMINI_API_KEY set

Step 6: Check state machine
        Add: logger.debug(f"State: {convo.state}")
        → Check state transitions valid

Step 7: Check response sending
        tail -f logs/editorbot.log
        → Watch for sending message

Step 8: Check Telegram received it
        Open Telegram app
        → See bot response

✅ If you see response in Telegram, everything works!
```

---

**Created:** 2025-01-17  
**Purpose:** Visual understanding of EditorBot architecture  
**Use:** Print & pin on wall, or keep in IDE
